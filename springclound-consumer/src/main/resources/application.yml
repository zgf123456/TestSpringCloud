server:
  port: 8010

spring:
  application:
    name: springclound-eureka-consumer

eureka:
  client:
    service-url:
      default-zone :  http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
